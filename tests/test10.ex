"""
  This is a whole simple program with my
  define tokens
"""

# A simple function
def process(args) do
  # A constant
  @cont "0"
  for y <- args do
      for z <- y do
        if y < 7 do
          password = "alumno"
          if 9.31 > 10 do
            f = false
          end
        else
          password = "nelpastel" + cont
        end
    end
  end

end
